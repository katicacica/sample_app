<li>
  <%= gravatar_for user, :size => 30 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
  | <%= link_to "Törlés", user, :method => :delete, :confirm => "Biztos?",
                                :title => "Delete #{user.name}" %>
  <% end %>
</li>
